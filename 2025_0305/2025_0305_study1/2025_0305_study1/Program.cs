using System;
using System.Collections.Generic;
using System.ComponentModel.Design;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace _2025_0305_study1
{
    class Program
    {
        static void Main(string[] args)
        {
            //string[] names = { "Charlie", "Alice", "Bob" };//한글도 잘 됨
            //var sorting = names.OrderBy(x => x);
            ////var rSorting = names.OrderByDescending(x => x);

            //foreach (var name in sorting)
            //{
            //    Console.WriteLine(name);
            //}
            ////foreach (var name in rSorting)
            ////{
            ////    Console.WriteLine(name);
            ////}

            //var firstName = names.FirstOrDefault(n => n.StartsWith("A"));
            //Console.WriteLine($"First name starting with A : {firstName}");

            ////메소드 구문, 쿼리 구문
            //int[] nums = { 5, 3, 8, 1, 4 };

            ////메서드구문
            //var sortedMeshod = nums.OrderBy(x => x);

            ////쿼리
            //var sortedQuery = from n in nums
            //                  orderby n
            //                  select n;

            //Console.WriteLine("Method synstax");
            //foreach(var n in sortedMeshod)
            //{
            //    Console.WriteLine(n);
            //}

            //Console.WriteLine("Query syntax");
            //foreach (var n in sortedQuery)
            //{
            //    Console.WriteLine(n);
            //}

            //string[] words = { "apple", "banana", "cherry" };

            ////Select
            //var lenghts = words.Select(w => w.ToUpper());

            //foreach (var len in lenghts)
            //{
            //    Console.WriteLine(len);
            //}
        }
    }
}
